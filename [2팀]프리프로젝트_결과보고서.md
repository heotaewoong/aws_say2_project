# [2팀] 프리프로젝트 결과보고서

## Rare-Link AI: 멀티모달 희귀 폐 질환 진단 보조 시스템

| 항목 | 내용 |
|---|---|
| **팀명** | 2팀 (성균관대학교 AWS 바이오헬스케어 2기) |
| **프로젝트명** | Rare-Link AI |
| **기간** | 2026.03.19 ~ 2026.04.13 (Pre-Project 단계) |
| **멘토** | 이희찬 (AWS AINATION) |
| **팀원** | 박성수(팀장), 허태웅(모델), 양희인(데이터/백엔드), 배기태(데이터/모델), 권미라(데이터/발표) |

---

## 1. 프로젝트 개요

### 1.1 배경 및 목적

희귀 폐 질환은 전체 폐 질환의 약 5% 미만으로, 전문의 부족과 진단 지연으로 인한 치료 시기 상실이 주요 문제이다. 본 프로젝트는 **흉부 X-ray 영상, 임상 소견서(NLP), 혈액검사(Lab), 유전자 정보**를 통합하여 희귀 폐 질환 가능성을 자동으로 평가하고, 의사에게 근거 기반 진단 리포트를 제공하는 **멀티모달 AI 진단 보조 시스템**을 구축하는 것을 목표로 한다.

### 1.2 시스템 아키텍처

```
환자 데이터 입력
    ├── Agent A (Vision): 흉부 X-ray → 14개 병변 확률 + HPO 코드
    ├── Agent B (NLP): 의사 소견서 → LLM(llama3.1) → HPO 코드 추출
    ├── Agent C (Lab): 혈액검사 수치 → 이상 지표 → HPO 코드
    │
    ▼ HPO 프로필 통합
    ├── Agent E (Inference): Orphanet 지식베이스 기반 IDF 가중 희귀질환 매칭
    │
    ▼
    └── Agent D (Reporter): 의사용 임상 리포트 생성
```

### 1.3 사용 데이터

| 데이터셋 | 용도 | 규모 | 출처 |
|---|---|---|---|
| **MIMIC-CXR** | 흉부 X-ray 영상 + CheXpert 라벨(14개) | ~227,000장 / 65,379명 | MIT (PhysioNet) |
| **MIMIC-IV** | 혈액검사(Lab), 임상 소견서 | 76,610명 | MIT (PhysioNet) |
| **CheXpert** | 추가 학습 데이터 (14개 라벨) | ~224,316장 / 65,240명 | Stanford AIMI |
| **Orphanet (Orphadata)** | 희귀질환-HPO-유전자 매핑 지식베이스 | 536개 폐 관련 질환 | Orphanet |

---

## 2. 모델 개발 (Vision Agent)

### 2.1 모델 구조

- **Backbone**: DenseNet-121 (ImageNet-1K 사전학습)
- **입력**: 448x448 RGB (CLAHE 대비 강화 적용)
- **출력**: 14개 병변 sigmoid 확률 (멀티라벨 분류)
- **손실함수**: BCEWithLogitsLoss + pos_weight + -1 라벨 마스킹
- **옵티마이저**: Adam (lr=1e-4), ReduceLROnPlateau
- **학습 인프라**: AWS SageMaker ml.g4dn.16xlarge Spot Instance (4x NVIDIA T4)

### 2.2 전처리 파이프라인

```
원본 X-ray (다양한 해상도)
    → CLAHE 대비 강화 (clipLimit=2.0, tileGrid=8x8)
    → 448x448 Resize
    → ImageNet Normalize
    → (학습 시) RandomHorizontalFlip
```

### 2.3 데이터 불균형 처리

| 기법 | 설명 |
|---|---|
| **No Finding 언더샘플링** | 정상 이미지를 5,000장으로 제한 (기존 ~100K) |
| **WeightedRandomSampler** | 희귀 질환 클래스 오버샘플링 (최대 pos_freq 역수 가중치) |
| **pos_weight** | 양성/음성 비율 기반 가중치 (상한 5.0) |
| **-1 라벨 마스킹** | 불확실 라벨(-1)을 손실 계산에서 제외 |
| **Per-class Threshold** | 클래스별 F1 최대화 기준 최적 임계값 자동 탐색 |

---

## 3. 실험 결과

### 3.1 14클래스 멀티라벨 분류 실험 이력

| 차수 | 데이터 | 주요 변경 | mAUROC | Macro F1 | 비고 |
|---|---|---|---|---|---|
| **2차** | 20K (언더샘플링) | CLAHE + pos_weight | 0.7257 | 0.35 | Epoch 9 |
| **3차** | 20K + WeightedRandomSampler | 희귀질환 오버샘플링 | 0.7285 | 0.38 | Epoch 4 |
| **4차** | 220K (전체 MIMIC) | 전체 데이터 스케일업 | **0.7699** | 0.40 | **Best, Epoch 2** |
| **5차(a)** | ConvNeXt-L (198M) | 대형 백본 시도 | - | - | Focal Loss 실패 |
| **5차(b)** | Balanced CSV (사전분리) | 균형잡힌 CSV 직접 사용 | 0.7635 | **0.4778** | F1/Prec/Rec 향상 |

> **핵심 발견**: 데이터 스케일업(20K → 220K)이 mAUROC +0.044로 가장 큰 성능 향상을 가져왔으며, Balanced CSV 적용 시 mAUROC는 소폭 하락하나 F1/Precision/Recall이 유의미하게 개선되었다.

### 3.2 2클래스 이진 분류 실험 (Pleural Effusion vs Pneumothorax)

| 지표 | 값 | 비고 |
|---|---|---|
| **AUROC** | 0.8863 | 14클래스 대비 +0.12 |
| **Accuracy** | 0.8125 | - |
| **F1-Score** | 0.8101 | - |
| **Precision** | 0.8205 | - |
| **Recall** | 0.8000 | - |

- 학습 데이터: 1,600장 (800장 x 2클래스, 완전 균형)
- Best epoch: 13/15
- **결론**: 클래스 수 축소 + 데이터 균형 맞춤 → 성능 대폭 향상

### 3.3 3클래스 분류 실험 (Edema / Lung Opacity / Pneumonia)

| 실험 | 데이터 | mAUROC | 비고 |
|---|---|---|---|
| 실험1 (PE/LO/CO) | 1,200장 | 0.6931 | LO/CO 임상적 혼동 |
| 실험2 (ED/LO/PN) | 2,200장 x 3 | 0.7581 | 체크포인트 오염 의심 |
| 실험3 (계획) | 2,200장 x 3 | - | AdamW + clean 재학습 예정 |

- **실험1 실패 원인**: Consolidation과 Lung Opacity는 영상에서 시각적으로 매우 유사
- **실험2**: 체크포인트 오염으로 신뢰성 부족, 재실험 필요

### 3.4 실패한 시도 및 교훈

| 시도 | 결과 | 원인 분석 |
|---|---|---|
| **ASL (Asymmetric Loss)** | Loss 0으로 수렴 | MIMIC 노이즈 라벨 + 극단적 음성 우세 |
| **Focal Loss** | Gradient 소멸 | 동일 원인, 감마 파라미터 조정도 실패 |
| **ConvNeXt-L 백본** | 학습 불안정 | 198M 파라미터 과적합 + Focal Loss 결합 |

---

## 4. 시스템 구현 현황

### 4.1 Vision Agent (Agent A)

| 항목 | 구현 상태 |
|---|---|
| DenseNet-121 기반 14클래스 분류 | 완료 |
| CLAHE 전처리 | 완료 |
| Grad-CAM 병변 위치 시각화 | 완료 |
| HPO 코드 자동 매핑 (14개 라벨 → HPO) | 완료 |
| SageMaker Spot 학습 파이프라인 | 완료 |
| 2클래스/3클래스 분리 학습 | 진행 중 |

### 4.2 NLP Agent (Agent B)

| 항목 | 구현 상태 |
|---|---|
| 로컬 LLM(llama3.1) 기반 소견서 분석 | 완료 |
| 임상 표현 → HPO 코드 자동 추출 | 완료 |

### 4.3 Lab Agent (Agent C)

| 항목 | 구현 상태 |
|---|---|
| MIMIC-IV 혈액검사 데이터 76,610명 로드 | 완료 |
| 정상 범위 이탈 → HPO 코드 매핑 | 완료 |

### 4.4 추론 엔진 (Agent E)

| 항목 | 구현 상태 |
|---|---|
| Orphanet 기반 536개 폐 질환 지식베이스 | 완료 |
| IDF 기반 가중 매칭 스코어링 | 완료 |
| 에이전트 확신도 반영 정밀 매칭 | 완료 |

### 4.5 리포터 (Agent D) & 웹 UI

| 항목 | 구현 상태 |
|---|---|
| 의사용 임상 리포트 자동 생성 | 완료 |
| Streamlit 대시보드 (app.py) | 완료 |

---

## 5. AWS 인프라 활용

| AWS 서비스 | 용도 | 태그 규칙 |
|---|---|---|
| **SageMaker** | DenseNet-121 GPU 학습 (Spot Instance) | `pre-sagemaker-2-2-team` |
| **S3** | 학습 데이터, CSV, 모델 가중치 저장 | `pre-s3-2-2-team` |
| **EC2** | 개발 및 테스트 환경 | `pre-ec2-2-2-team` |

- **S3 버킷**: `say2-2team-bucket`
- **인스턴스**: ml.g4dn.16xlarge (4x NVIDIA T4, 64GB GPU VRAM)
- **스팟 인스턴스**: 체크포인트 저장 + 자동 복원으로 비용 최적화
- **IAM Role**: `SKKU_SageMaker_Role`

---

## 6. Vision 브랜치 (aws_say2_project_vision)

별도 연구용 vision 브랜치에서 SooNetEngine 개발:

| 항목 | mini_project (서비스) | vision 브랜치 (연구) |
|---|---|---|
| 입력 채널 | RGB 3채널 | **Grayscale 1채널** |
| 해상도 | 256→224px | **448px** |
| 전처리 | ImageNet Normalize | **TXV 스케일링 (px*2048-1024)** |
| U-Net 폐 분할 | 없음 | **포함** |
| 학습 데이터 | MIMIC-CXR only | **MIMIC + CheXpert** |

### 사전학습 가중치

| 파일 | 학습 데이터 |
|---|---|
| `chexnet_1ch_448_chexpert_best.pth` | CheXpert (1채널, 448px) |
| `chexnet_chexpert_frontal_best.pth` | CheXpert Frontal |
| `chexnet_mimic_best.pth` | MIMIC-CXR |
| `unet_lung_mask_ep10.pth` | 폐 분할 U-Net |

---

## 7. 향후 계획 (Final 단계)

### 7.1 모델 성능 개선

1. **모델 병합**: mini_project + vision 브랜치 SooNetEngine 통합
2. **레이블 축소 학습**: 2/3/5 클래스 분리 학습 후 앙상블
3. **EfficientNet-B4 백본 시도**: DenseNet-121 대체
4. **Label Smoothing**: 노이즈 라벨 대응 (0→0.05, 1→0.95)
5. **LDAM-DRW Loss**: 희귀 클래스 마진 확대

### 7.2 데이터 고도화

1. CheXpert + MIMIC 통합 학습 (약 450K 이미지)
2. Orphanet 지식베이스 정확도 검증 (lung 관련 double-check)
3. Radiology 소견 누락분 보충

### 7.3 시스템 고도화

1. 정상 탐지 모델 파이프라인 전단 배치 (false negative 최소화)
2. 서로 영향을 안 주는 증상 묶어서 분리 학습 (모델 3~4개 앙상블)
3. 아키텍처 최종 확정 (중간발표 전)

---

## 8. 참고 논문 및 자료

1. Rajpurkar, P. et al. (2017). **CheXNet**: Radiologist-Level Pneumonia Detection on Chest X-Rays with Deep Learning. _arXiv:1711.05225_
2. Irvin, J. et al. (2019). **CheXpert**: A Large Chest Radiograph Dataset with Uncertainty Labels and Expert Comparison. _arXiv:1901.07031_
3. Johnson, A. et al. (2019). **MIMIC-CXR**: A Large Publicly Available Database of Labeled Chest Radiographs. _arXiv:1901.07042_
4. Ronneberger, O. et al. (2015). **U-Net**: Convolutional Networks for Biomedical Image Segmentation. _MICCAI 2015_
5. Cao, K. et al. (2019). **LDAM-DRW**: Learning Imbalanced Datasets with Label-Distribution-Aware Margin Loss. _NeurIPS 2019_

---

| 작성일 | 2026.04.09 |
|---|---|
| 작성자 | 2팀 (박성수, 허태웅, 양희인, 배기태, 권미라) |
