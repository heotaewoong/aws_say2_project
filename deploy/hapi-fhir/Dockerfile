FROM hapiproject/hapi:v7.4.0

# No modifications needed - the base image is distroless (no shell).
# Password injection is handled by the host-level start.sh script
# which fetches the secret and passes it as an environment variable
# to docker compose.
